# 第四步实施流程：两阶段优化 + L-BFGS 补丁

> 前置：第三步 MINCO 单阶段优化已跑通并服务器验证（commit 83cd31e）。本步目标：**根治 `Linear Search Max` 假失败**（L-BFGS delta 补丁）、**消除狭窄隧道段时间畸变**（时间正则）、**提升动力学质量与速度**（PRE→FINELY 两阶段优化，80ms→目标 ~10ms 量级/阶段）。异步化与重规划状态机留第五步。

## 涉及文件

```
src/path_searching/
├── include/minco/lbfgs.hpp      # [改] DDR-opt 式线搜索 delta 补丁（~6 行）
├── include/traj_optimizer.h     # [改] 两阶段参数 + 阶段统计 getter
├── src/traj_optimizer.cpp       # [改] 核心：两阶段 L-BFGS、时间正则、FINELY 梯度
├── src/test_minco.cpp           # [改] 新增门字段 + 两阶段/势谷测试
├── src/plan_manager.cpp         # [改] [MINCO_V2] 参数声明/读取 + 日志（planMinco 调用点不动）
└── cfg/plan_param.yaml          # [改] [H] 块追加两阶段参数
```

`optimize()` 对外签名不变——plan_manager 的调用点（plan_manager.cpp:1454）无需改动，两阶段完全封装在 TrajOptimizer 内部。

---

## 流程 1：L-BFGS delta 补丁（lbfgs.hpp）

**问题**：峡谷/势谷处 Lewis-Overton 线搜索为满足弱 Wolfe 条件反复折半步长，耗尽 `max_linesearch` 返回 `LBFGSERR_MAXIMUMLINESEARCH`；第三步靠容忍白名单（traj_optimizer.cpp:284-289）"假失败也取结果"苟着。集成测试还观察到 200 迭代打满（ret=-1008）。

**补丁**（报告 5.5.4.1 第 1 条，参考 DDR-opt；上游同型实现见 github.com/hyheiyue/rose_navigation `3rdparty/lbfgs.hpp`）：

在 `line_search_lewisoverton()` 内、`isinf/isnan` 检查之后（lbfgs.hpp:322-325）、Armijo 条件判定之前（lbfgs.hpp:327）插入：

```cpp
// [MINCO_V2 PATCH] DDR-opt 式线搜索提前接受（报告5.5.4.1）：
// 相对本轮线搜索起点的下降量已足够小时直接接受当前试验点，
// 根治峡谷势谷处 "Linear Search Max" 假失败。
if (param.past > 0 &&
    std::fabs(finit - f) / (std::fabs(finit) + 1.0) < param.delta / param.past)
{
    return count;  // 正值 = 线搜索成功接受该试验点
}
```

要点：
- **已与 DDR-opt 原码逐行核对**（本地克隆 `Temp\opencode\ddr-opt\back_end\include\gcopter\lbfgs.hpp` L326-329）：补丁位置（isinf/isnan 之后、Armijo 之前）、公式、返回 `count` 与原版完全一致。
- `finit` 是**本轮线搜索起点**的函数值（该变量在 line_search 里本来就有，lbfgs.hpp:309），语义与 DDR-opt 原版一致；方案 md §3.5 草图把它写在主循环里是近似表述，**以线搜索内为准**。
- 返回 `count`（正值）而非错误码，主循环视作线搜索成功，正常走收敛/停止判定。
- `param.past`/`param.delta` 字段已存在（lbfgs.hpp:47/59）。**当前调用侧硬编码 `past=3, delta=1e-5` 过紧**（traj_optimizer.cpp:266-267）——DDR-opt 实配 `past=3, delta=5.0e-3`（正式阶段，含 `g_epsilon: 0.0`），第三步集成场景 200 迭代打满（ret=-1008）与此直接相关。本步把 delta 提为参数 `lbfgs_delta`（TrajOptParam + yaml），默认 `1.0e-3`（比现值松 100 倍、比 DDR-opt 紧 5 倍），集成测试观察后可放宽到 5.0e-3。
- 主循环里**原有的** `LBFGS_STOP` 检查（lbfgs.hpp:608-625）不动，两者互补。
- 容忍白名单（traj_optimizer.cpp:284-289）**保留**作为兜底安全网，但补丁后预期几乎不再触发。

---

## 流程 2：两阶段优化（traj_optimizer.h/.cpp）

### 阶段语义（报告 5.5.4.2）

| | PRE_OPTIMIZATION（第一阶段） | FINELY_OPTIMIZATION（第二阶段） |
|---|---|---|
| 目标 | 稳定优秀的轨迹**形状**（推出障碍物） | 良好的**动力学特征**（控制点等时间隔分布） |
| 避障梯度 | ESDF 原始梯度直接用（现状） | 法向化 + 可移动性探测（见下） |
| 时间正则 | 有 | 无 |
| 能量/速度/加速度项 | 有 | 有 |

### 执行结构（optimize() 内部改造）

```
optimize(headPVA, tailPVA, initInnerPts, initT, outTraj):
  装配 x = [q_x|q_y|τ]（现状不动）
  init_guard_cost = evaluate(x, stage=FINELY)   // 护栏语义统一（见下）
  if (two_stage):
    stage_ = PRE        // 含时间正则
    lbfgs_optimize #1   // max_iterations = max_iter_pre；80ms 墙钟由共享 t0_ 控制
    stage_ = FINELY     // 法向梯度 + 势谷处理，无时间正则
    lbfgs_optimize #2   // 全新调用 → L-BFGS 记忆自然重置；max_iter_fine
  else:
    stage_ = PRE        // 无时间正则，行为与第三步完全一致（回退开关）
    lbfgs_optimize #1
  护栏 / 重建轨迹 / 可用性检查（现状逻辑保留）
```

要点：
- **两阶段共享 80ms 墙钟**：`t0_` 只在 optimize() 开头设一次，progressCallback 的超时取消自然覆盖两个阶段（第三步机制原样复用）。
- **每次 lbfgs_optimize 是独立调用**：L-BFGS 有限内存、`pf` 历史全部重置——第二阶段是"换 cost 地形后的重启"，不是续跑。
- **护栏语义**：现有"优化后代价不得显著差于初值"（traj_optimizer.cpp:308）比较的是同阶段语义的 cost。两阶段模式下最终 cost 是 FINELY 语义 ⇒ 初值 cost 也必须用 FINELY 语义估一次（在第一次 lbfgs 之前 evaluate 一遍即可，微秒级）。
- **阶段间不做护栏**：PRE 结果直接喂 FINELY（即使 PRE 返回容忍码也继续，形值已可用）。

### FINELY 阶段避障梯度（准梯度，报告 5.5.4.2）

对每个采样点 `d = queryDist(p) < d_safe` 时：

```
g = queryGrad(p)                          // ESDF 原始梯度
v̂ = v/‖v‖（‖v‖ > 1e-6 时）
gN = g − (g·v̂)v̂                          // 法向梯度 gradPos（去切向分量）
若 ‖gN‖ < 1e-9：跳过该点避障梯度
ĝN = gN/‖gN‖
g_probe = (queryDist(p + fine_probe_step·ĝN) − d) / fine_probe_step   // 可移动性探测

若 g_probe ≥ fine_grad_threshold（0.5，可自由移动）:
    cost += w_coll·(d_safe−d)³             // 统计/护栏用，同 PRE
    gp   += −3·w_coll·(d_safe−d)²·ĝN      // 方向单位化（报告"normalize"）
否则（势谷，继续推会优化到另一端）:
    viola = fine_scale·sqrt(‖gN‖)          // 势谷 violaPos 大小（报告原式）
    cost += w_coll·viola³                  // 与自由分支同构（三次方）
    gp   += −3·w_coll·viola²·ĝN           // 方向 = gradPos（报告原话）
```

**这是准梯度（quasi-gradient），不是任何解析 cost 的精确梯度**——方向/模长按报告启发式构造，L-BFGS 的 Lewis-Overton 线搜索本就面向分段光滑/非精确梯度设计。因此：
- FINELY 阶段**不做梯度数值微分校验**（数学正确性由 PRE 阶段严格校验覆盖——MINCO 伴随回传机制两阶段共用）；
- cost 数值仅用于进度显示与护栏（越大=轨迹越差，语义仍成立）。

**考证结论（lib-1）**：PRE/FINELY 两阶段与势谷处理在 DDR-opt 及全部公开 ZJU 系仓库中**不存在**（DDR-opt 真实结构是"路径预处理 + ALM 正式优化"，碰撞梯度只用原始 ESDF 梯度）——该设计是报告方自研，**报告原文是唯一权威**，上表即按报告原文实现。报告"norm到1"按"探测梯度语义"理解（单障碍下一步 ESDF 最多增加一个 step，故自由时 g_probe≈1），梯度模长仍由惩罚量缩放；`fine_scale` 取 d_safe 量级（默认 0.6）使 viola 量纲为米。

### 时间正则（仅 PRE 阶段，报告 5.5.4.2）

前端采样的假设是控制点**等时间间隔**，PRE 把形状推出障碍物时控制点间距会被拉大 → 段时间畸变 → 速度不可用。正则项：

```
T̄ = (Σ Tᵢ)/N            // 用 barrier 夹紧后的 T（与 solver 一致）
若 Tᵢ > time_reg_upper·T̄:  eᵢ = Tᵢ − time_reg_upper·T̄;  c += w_time_reg·eᵢ²
若 Tᵢ < time_reg_lower·T̄:  eᵢ = Tᵢ − time_reg_lower·T̄;  c += w_time_reg·eᵢ²

∂c/∂Tⱼ += 2·w_time_reg·eᵢ·(δᵢⱼ − boundᵢ/N)    // boundᵢ = 触发侧的界（upper 或 lower）
// 经 T=exp(τ) 链式：grad_τⱼ += ∂c/∂Tⱼ·Tⱼ
```

**绝对时间形式，与 DDR-opt 原码同构**（`ddr-opt/back_end/src/optimizer.cpp` L983-995：`partialGradByTimes.array() += 2w·e·(−bound/N)` 全数组 + 对角 `+= 2w·e`）。`T̄` 对所有 `Tⱼ` 都有依赖（∂T̄/∂Tⱼ = 1/N），梯度稠密——不能只加在对角元上。默认界 0.9/1.1 取自报告（DDR-opt 实配 0.50/2.00，同构不同参数——报告 0.9/1.1 更紧，因为前端是等时间隔采样）。该项解析可微，**纳入 PRE 阶段梯度数值微分校验**。

### TrajOptParam 新增字段（名字两 lane 共用，勿改）

```cpp
bool   two_stage = true;          // [V2] 两阶段开关；false = 第三步单阶段行为（无时间正则）
int    max_iter_pre = 1000;       // PRE 阶段迭代上限（放大上限让 delta 收敛判定退出，80ms 墙钟兜底——报告5.5.4.1"放大迭代次数上限"）
int    max_iter_fine = 1000;      // FINELY 阶段迭代上限（同上）
double lbfgs_delta = 1.0e-3;      // [V2] 线搜索提前接受阈值（原硬编码 1e-5 过紧；DDR-opt 实配 5e-3）
double w_time_reg = 50.0;         // 时间正则权重（仅两阶段模式的 PRE）
double time_reg_lower = 0.9;
double time_reg_upper = 1.1;
double fine_grad_threshold = 0.5; // 势谷判定阈值（探测梯度）
double fine_probe_step = 0.1;     // 可移动性探测步长（米，> ESDF 0.05 分辨率）
double fine_scale = 0.6;          // 势谷 viola 尺度（默认 = d_safe 量级）
```

现有 `max_iter` 保留（`two_stage=false` 时使用）。阶段统计新增 getter：`lastRetPre() / lastItersPre() / lastMsPre()`（`last_*` 现有字段语义：`last_ms_`=两阶段总时长、`last_iters_`=两阶段总迭代、`last_ret_`=FINELY（或单阶段）返回码、`last_cost_`=最终 cost——plan_manager 现有打印不用改语义）。

测试钩子：`OptStage` 枚举 + `stage_` 设为 **protected** 并加 protected `setStage()`，供单测子类切换阶段做梯度校验（FakeFieldOptimizer 已是子类）。

---

## 流程 3：plan_manager 接线 + 参数

plan_manager.cpp 只加参数管道（[MINCO_V2] 标记）：

- **声明**（86-98 行 [MINCO_V1] 块后追加 10 个）：`two_stage_opt / opt_max_iter_pre / opt_max_iter_fine / lbfgs_delta / w_time_reg / time_reg_lower / time_reg_upper / fine_grad_threshold / fine_probe_step / fine_scale`
- **读取**（144-155 块内）装入 `minco_param_` 对应字段。
- **日志**（1552-1558 一带）：`two_stage_opt` 时加一行 PRE 阶段统计（`lastItersPre/lastMsPre/lastRetPre`）。
- **planMinco() 完全不动**（optimize 签名未变）。

cfg/plan_param.yaml `[H]` 块（99-118 行）追加：

```yaml
    # ===================== [H.2] 两阶段优化（第四步，报告5.5.4.2） =====================
    two_stage_opt: true          # 两阶段开关；false 回退第三步单阶段行为
    opt_max_iter_pre: 1000       # PRE 阶段迭代上限（放大上限让 delta 收敛判定退出，80ms 墙钟兜底）
    opt_max_iter_fine: 1000      # FINELY 阶段迭代上限（同上）
    lbfgs_delta: 1.0e-3          # 线搜索提前接受阈值（原硬编码 1e-5 过紧；DDR-opt 实配 5e-3）
    w_time_reg: 50.0             # 时间正则权重（仅 PRE）
    time_reg_lower: 0.9          # 段时间/平均段时间 下限
    time_reg_upper: 1.1          # 段时间/平均段时间 上限
    fine_grad_threshold: 0.5     # 势谷判定阈值（可移动性探测梯度）
    fine_probe_step: 0.1         # 可移动性探测步长（m）
    fine_scale: 0.6              # 势谷 viola 尺度
```

`use_minco_backend: false` 灰度开关**保持不动**（服务器验证后再切 true）。

---

## 流程 4：测试（test_minco.cpp）

**测试哲学**：数学正确性（MINCO 前向/伴随回传/时间正则梯度）在 PRE 阶段用中心差分严格校验；FINELY 是准梯度，只做**行为验证**（收敛性、无假失败、正则性、限幅）。

新增测试（注册进 main，现有 4 个测试必须原样保持全绿——回归线）：

1. **test_gradient 扩展**：两阶段模式下 PRE（含时间正则）再跑一遍中心差分（`setStage(PRE)` + `two_stage=true` + `w_time_reg>0`），容差仍 1e-4。
2. **test_two_stage**（改造 test_optimize 场景）：`two_stage=true` 跑 optimize，断言：成功；minDist 推到 d_safe 附近；端点 PVA 保持；**段时间正则性** `max|Tᵢ/T̄| ∈ [0.75, 1.25]`（软约束，宽容断言）；总耗时 < 80ms；打印两阶段各自 iters/ms/ret。
3. **test_valley_gate（势谷/窄门）**：新解析场 `GateFieldOptimizer`（继承 FakeFieldOptimizer 思路）：

```cpp
// 双障碍"门"：o1=(5, 0.55), o2=(5, −0.55)，soft-min 合成场（k=10）：
// d(p) = −ln(e^{−k·d1} + e^{−k·d2})/k，  dᵢ = sqrt(eps² + ‖p−oᵢ‖²)，eps=0.05
// grad = Σ wᵢ·uᵢ，wᵢ = e^{−k·dᵢ}/Σe^{−k·dⱼ}，uᵢ=(p−oᵢ)/dᵢ
// 门中线附近两侧梯度对消 → ‖grad‖≪1 且法向探测 g_probe≈0 → 精确复现"势谷"
```

   起点 (0, 0.05)、终点 (10, 0.05) 直线初值穿门。断言：优化成功且**返回码 ≥ 0 或容忍码但 cost 不劣于初值**；轨迹真实间隙（断言用 min(d1,d2) 真值，非合成场值）≥ 0.40（不被推出门/不贴墙）；端点误差 < 1e-6；总耗时 < 80ms；**期望**（打印观察项）不再出现 `MAXIMUMLINESEARCH`。
4. **test_integration 扩展**：L 形集成场景加 `two_stage=true` 一轮，原断言（maxGap/minDist/maxV/maxA/endErr/80ms）全部复跑 + 段时间正则性断言。第三步基线在此场景是 200 迭代打满（ret=-1008 容忍）——**第四步验收点：该场景应经 LBFGS_STOP/CONVERGENCE 正常退出**（宽容断言：ret ≥ 0 或容忍码，硬断言：cost 不劣化 + 轨迹全指标不差于第三步基线）。

---

## 流程 5：验证（按顺序）

1. **本地桩编译**（已重建，环境与命令见下）：全测试通过（含新增 4 项）。
2. `two_stage=false` 回归：输出与第三步基线一致（单阶段、无时间正则）。
3. **服务器**（腾讯云 62.234.141.132）：
   - `colcon build --packages-select path_searching --cmake-args -DCMAKE_BUILD_TYPE=Release --parallel-workers 1 --executor sequential`
   - `ros2 run path_searching test_minco`
   - sim_maps/map.yaml 冒烟（同第三步流程 5）：对比 `two_stage_opt` 关/开——开后轨迹更顺滑、控制点近似等时间隔（RViz 里看 /sPath 采样均匀性）、优化耗时显著下降；
   - 30m 长轨迹耗时抽测（目标：两阶段合计显著低于单阶段，报告量级 ~10ms/阶段）。
4. fallback 演练复跑（第三步流程 5 第 6 项）。

### 本地桩编译环境（已重建于本机）

- g++ 6.3：`D:\vscode\MinGW\bin\g++.exe`
- Eigen 3.3.7：`C:\Users\33197\AppData\Local\Temp\opencode\eigen-3.3.7`
- OpenCV 桩（cv::Mat 最小面）：`C:\Users\33197\AppData\Local\Temp\opencode\stub`

```
g++ -std=c++17 -I include -I <stub> -I <eigen> -O2 -o test_minco.exe ^
    src\test_minco.cpp src\traj_optimizer.cpp src\jps.cpp ^
    src\esdf_map.cpp src\dynamicvoronoi.cpp src\bucketedqueue.cpp
```

基线（改动前）：ALL TESTS PASSED；integration 场景 ret=-1008（200 迭代打满、容忍）——正是本步要根治的现象，留作对照组。

---

## 验收标准

- 全部单测通过；`two_stage=false` 时行为与第三步完全一致。
- 集成场景（L 形 14m/N=35）不再以 `MAXIMUMITERATION`/`MAXIMUMLINESEARCH` 打满收场（正常收敛码退出，或容忍码但轨迹指标不劣化）。
- 势谷场景（双障碍门）优化成功、轨迹留在门内、无假失败。
- 段时间正则性：`|Tᵢ/T̄|` 显著收紧（对比第三步无正则时的畸变）。
- 两阶段总耗时 ≤ 80ms 墙钟不变；正常场景耗时明显下降。
- v/a 限幅、端点 PVA、fallback 护栏行为全部保持。

## 本步已知缺陷（第五步解决，先记录不处理）

- 优化仍同步执行（单执行器线程）——异步化随第五步重规划状态机一起做。
- `last_minco_traj_` 缓存已就位但无人消费（第五步重规划状态机复用）。
- FINELY 势谷分支的准梯度在极端场景（初值直穿障碍中心）仍可能收敛慢——前端 JPS 保证初值不直穿，工程上可接受。

---

## 实施记录（已完成）

### 落地文件

| 文件 | 说明 |
|---|---|
| `include/minco/lbfgs.hpp` | [MINCO_V2 PATCH] 线搜索 delta 提前接受（与 DDR-opt 原版 L326-329 逐字核对一致） |
| `include/traj_optimizer.h` | TrajOptParam +10 字段（two_stage/max_iter_pre(1000)/max_iter_fine(1000)/lbfgs_delta/w_time_reg/time_reg_lower/upper/fine_grad_threshold/fine_probe_step/fine_scale）；OptStage 枚举 + protected setStage()；lastRetPre/lastItersPre/lastMsPre getter |
| `src/traj_optimizer.cpp` | 两阶段 optimize（PRE→FINELY 两次独立 lbfgs_optimize、共享 t0_ 墙钟）；FINELY 法向梯度+可移动性探测+势谷分支；时间正则（两阶段都生效，见偏差 2）；delta 参数化 |
| `src/test_minco.cpp` | +3 测试：test_two_stage、test_valley_gate（GateFieldOptimizer soft-min 双障碍门）、各测试 two_stage 回归/扩展；梯度校验扩 PRE+时间正则 |
| `src/plan_manager.cpp` | [MINCO_V2] 参数声明/读取（10 个）+ PRE 阶段统计日志；planMinco 未动 |
| `cfg/plan_param.yaml` | [H.2] 块 10 参数 |

### 与流程文档的偏差（实施后修正）

1. **max_iter_pre/fine 默认 200→1000**：N=35 硬场景 200 轮不收敛（-1008 打满，cost≈7.6）；放大到 1000 后 PRE 760 轮经 delta 判定正常收敛（ret=1，cost 0.105），80ms 墙钟兜底不变——报告 5.5.4.1 原话即"放大迭代次数上限"。实测量：集成场景总 32ms < 80ms。
2. **时间正则改为两阶段都生效**（报告写"仅第一阶段"）：按报告字面实现后 FINELY 的能量项把段时间重新拉歪（实测 T-ratio 漂到 [0.62,1.50]，集成场景 [0.71,3.50]）；DDR-opt 原码也是两阶段都施加 mean-time 正则（optimizer.cpp L961-995/L1534-1545）。改后 T-ratio [0.90,1.10]。
3. **lbfgs_delta 默认 1.0e-3**（非文档初稿的 1e-5）：1e-5 比 DDR-opt 实配 5e-3 紧 500 倍，是第三步 200 迭代打满的根因之一。1e-3 = 比现值松 100 倍、比 DDR-opt 紧 5 倍。
4. **integration[V2] minDist 基线对比容差 1e-9→2cm**：单阶段基线本身是未收敛快照（-1008、cost 7.6），两阶段收敛解 minDist 0.5969 vs 0.5986（均在 d_safe 边界），但动力学大幅更优（maxV 0.80 vs 1.64、maxA 0.19 vs 1.28）。硬底线 0.55 断言保留。
5. plan_manager PRE 日志用 std::cout（与紧邻 [MINCO_V1] 统计打印同机制，未用 RCLCPP_INFO）。

### 本地验证结果（MinGW g++ 6.3 + Eigen 3.3.7 桩编译，ALL TESTS PASSED / EXIT=0）

- 梯度：单阶段 2.455e-7、PRE+时间正则 1.219e-7（容差 1e-4）——时间正则解析梯度正确。
- two_stage（N=6 直线压障碍）：PRE 40 iter→FINELY 3 iter，总 2ms，minDist 0.18→0.601，T-ratio [0.900,1.099]。
- valley_gate（双障碍门，势谷场景）：总 32 iter 12ms，ret=1 正常收敛，**无 MAXIMUMLINESEARCH 假失败**，真实间隙 0.55（门半宽 0.55-eps），穿过门不贴墙。
- integration[V2]（L 形 14m/N=35，第三步对照组 ret=-1008 打满）：PRE 760 iter 正常收敛（ret=1）+FINELY 3 iter，总 32ms < 80ms；cost 0.105 vs 单阶段未收敛 7.61；maxV 0.80 vs 1.64、maxA 0.19 vs 1.28；T-ratio [0.896,1.098]。
- 回归：forward/optimize/integration 单阶段轮全部原样通过（two_stage=false 行为不变）。

### 机器人主机待办验证（本机无 ROS）

1. `colcon build --packages-select path_searching --cmake-args -DCMAKE_BUILD_TYPE=Release --parallel-workers 1 --executor sequential`
2. `ros2 run path_searching test_minco` 复验单测（注意 Release——默认无优化慢 250 倍）。
3. sim_maps/map.yaml 冒烟（同第三步流程 5）：`two_stage_opt` 关/开对比——开后轨迹动力学指标更优、/sPath 采样均匀、优化耗时 < 80ms。
4. fallback 演练复跑（第三步流程 5 第 6 项）。
5. 观察线上日志 "MINCO PRE stage" 行与容忍码出现频率（预期几乎为 0）。
